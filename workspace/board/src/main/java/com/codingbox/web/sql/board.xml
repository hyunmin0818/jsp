<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
	<select id="getBoardList" parameterType="hashmap"
		resultType="boarddto">
		SELECT b.* FROM
		(SELECT rownum rn, a.* FROM
		(SELECT * FROM TBL_BOARD ORDER BY BOARDNUM desc) a) b
		WHERE b.rn BETWEEN ${startRow} AND ${endRow}
	</select>

	<select id="getBoardCnt" resultType="int">
		SELECT COUNT(*) FROM
		tbl_board
	</select>
	
	<insert id="InsertBoard" parameterType="boarddto">
		INSERT INTO tbl_board VALUES (board_seq.nextval, #{boardtitle}, #{boardcontents}, #{username}, sysdate, 0)
	</insert>
	
	<select id="getDetail" parameterType="_int" resultType="boarddto">
		SELECT * FROM tbl_board WHERE boardnum = #{boardnum}
	</select>
	
	<update id="updateReadCount" parameterType="_int">
		UPDATE tbl_board SET boardreadcount = boardreadcount + 1 WHERE boardnum = #{boardreadcount}
	</update>
</mapper>